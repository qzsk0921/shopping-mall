module.exports = {
  //保留两位
  checkedCheck: function (id, ids) {
    var flag = false
    if (ids.length) {
      for (var i = 0; i < ids.length; i++) {
        if (ids[i] == id) {
          flag = true
        }
      }
    }
    return flag
  }
}